import React from 'react';
import NoInformationScreen from '../common/NoInformationScreen';

const MasterReportScreen = ({ navigation }) => {
  return (
    <NoInformationScreen 
      navigation={navigation} 
      title="Master Reports" 
    />
  );
};

export default MasterReportScreen;
